# Binary game/mod files — never touch line endings, they're not text
*.vpulse binary
*.vmap binary
*.vpk binary
*.vpulse_c binary
*.vmap_c binary

# Plain text — normalize to LF in the repo, let git's autocrlf handle local checkout
*.md text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
